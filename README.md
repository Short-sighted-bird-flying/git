# git

一个初始化的 Git 仓库。

## 使用

```bash
git clone <repo-url>
cd git
```

## 目录结构

```
.
└── README.md
```

## 说明

本仓库由空目录初始化而来，作为项目起点。后续可以把代码、文档等文件加入此目录：

```bash
git add .
git commit -m "描述你的改动"
git push
```
